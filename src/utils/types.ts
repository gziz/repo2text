export interface WorkspaceFile {
  uri: string;
  path: string;
  name: string;
  relativePath: string;
}

export interface WorkspaceFolder {
  uri: string;
  path: string;
  name: string;
  relativePath: string;
}
